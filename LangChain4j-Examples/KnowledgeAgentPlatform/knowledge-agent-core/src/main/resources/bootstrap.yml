spring:
  config:
    import[0]: optional:nacos:knowledge-agent-core?group=KNOWLEDGE_AGENT
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        username: nacos
        password: nacos
        file-extension: yaml
